# Developer Lens Lab hosting reference

Reference-only preparation, 2026-09-10. This is not an executable prompt. The existing prompt library, continuation skill, governor, owner constitution and data policy remain authoritative. `manifest.json` is inert hosting-intake metadata, not a new research contract or capability grant.

The lab stays local. Approved synthetic reports may later be published as static artifacts, but the entire `.dllab` object store, raw runs, credentials, private allowlists and provider metadata must not become a website. No corpus collection, model invocation, promotion, data publication, domain, name or deployment trigger is changed here. Existing workflows may still run after a future merge.

LL1 defines a bounded synthetic publication allowlist using the current data policy. LL2 reuses the product-owned pinned method-trial projection rather than adding a competing schema or automatic promotion path. LL3 routes the work through existing task cards and the continuation workflow; do not create a second prompt library, governor, harness or collector.

Use invented fixtures only. A later display-name change must preserve package coordinates, schema IDs, CLI compatibility, scope-local object custody and product/lab ownership until a separately tested migration is approved. Keep unregistered naming candidates and operational account records outside the public repository.

## Verification

Syntax: `python -m json.tool .hosting/manifest.json`. Applicable context verification is `uv run dllab context verify`; future code/config milestones use the full gate in `CLAUDE.md`, including hygiene and strict documentation checks. Publication acceptance needs actual synthetic output inspection and product-contract validation. JSON validation establishes none of those outcomes.

Preserve the previous reviewed report/build as rollback. Do not label a synthetic showcase as evidence of real-repository validation or activate a new data class to make a demonstration look more realistic.
