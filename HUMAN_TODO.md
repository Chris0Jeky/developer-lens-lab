# Human actions

The initiating owner instruction on 2026-08-06 commissioned `Chris0Jeky/developer-lens-lab`, an
invented-data runnable bootstrap, and a reviewed synthetic contract seam with Developer Lens. A
newer instruction the same day explicitly made the repository public and chose a faster,
productivity-oriented sequence with non-essential security hardening documented for later. It did
not authorize a credential, private dataset, person-shaped analysis, or automatic model promotion.

- [ ] **q-1 — Choose a license before an open-source release.** The repository is public and
  intentionally has no `LICENSE` file, so it is source-visible but does not grant reuse rights.
  This does not block the public invented-data bootstrap; resolve it before packaging or promoting
  the project as open source.
- [ ] **q-2 — Approve any networked public-metadata corpus task.** Bootstrap performs no network
  collection. A future task must name the exact allowlist, fields, request/time/storage budgets,
  in-memory parsing and deletion behavior. The initial design has no token and no raw response
  landing. Approval for Developer Lens's existing `github.core` task does not silently transfer.
- [ ] **q-3 — Approve any generated dataset or trained-model artifact publication.** Public code,
  docs, tests, and invented small fixtures are approved through `origin`; generated datasets and
  run/model artifacts remain local and ignored until separately selected.
- [ ] **q-4 — Approve product integration or model promotion.** A reviewed EvaluationBundle may be
  proposed to Developer Lens, but no lab result installs itself or becomes a product claim.
- [ ] **q-5 — Decide whether a durable cross-repository artifact identity key may exist.** Until
  then, product commits and snapshot checksums are provenance only and may not become join keys.
- [ ] **q-6 — Reclassify the lab before storing C2/C3 or any real/private input.** T1 and
  `sensitive_data=false` are truthful only while the lab remains C0 invented-data-only.
